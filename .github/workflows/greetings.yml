name: Greetings

on:
  push: 
    branches: master 
    

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
    - name: greet
      run: echo "hello"
    
